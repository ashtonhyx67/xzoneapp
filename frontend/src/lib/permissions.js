// The permission names the API uses. Kept in step with backend/lib/groups.js —
// that file decides which group holds which of these.
export const PERMISSIONS = {
  VIEW_DIRECTORY: "viewDirectory",
  EDIT_DATABASE: "editDatabase",
  MANAGE_ACCOUNTS: "manageAccounts",
};
